{
  "name": "drawing-local-website-loop",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c yellow,cyan \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "build": "npm run build -w frontend"
  },
  "description": "Pad :5174 (/ canvas, /site MD reader). API :5175. RAG: python -m rag.cli seed|rebuild. ACI: content/CONTROL.md",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
